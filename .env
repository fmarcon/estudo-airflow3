# Arquivo .env para docker-compose
# Substitua pelos valores do seu usuário no host antes de subir os containers,
# por exemplo: export AIRFLOW_UID=$(id -u) && export AIRFLOW_GID=$(id -g)
# Ou edite este arquivo com os valores numéricos desejados.

# UID e GID usados pelos containers para mapear arquivos montados (dags, logs, plugins)
AIRFLOW_UID=1000
AIRFLOW_GID=1000
